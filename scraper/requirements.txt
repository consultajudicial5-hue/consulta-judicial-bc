# No external deps needed - uses stdlib only
# Optional: pip install beautifulsoup4>=4.12.0 lxml for better HTML parsing
# Optional: pip install supabase for native Supabase client (stdlib HTTP used by default)
